name: Tests
on:
  push:
    branches: [ rewrite ]
  pull_request:
    branches: [ rewrite ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: test/run --docker
